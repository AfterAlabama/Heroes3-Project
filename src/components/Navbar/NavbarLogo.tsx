import Typography from '@mui/material/Typography';

const NavbarLogo = () => {
	return (
		<Typography
			variant='h6'
			component='div'
			sx={{ flexGrow: 1, cursor: 'default' }}
		>
			HeroProgeny
		</Typography>
	);
};

export default NavbarLogo;
