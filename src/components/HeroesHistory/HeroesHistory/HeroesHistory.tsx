import Footer from '../../Footer/Footer';
import HistoryHeader from '../HeroesHistoryHeader/HistoryHeader';
import HistoryMenu from '../HeroesHistoryMenu/HistoryMenu';

const HeroesHistory = () => {
	return (
		<>
			<HistoryHeader />
			<HistoryMenu />
			<Footer />
		</>
	);
};

export default HeroesHistory;
